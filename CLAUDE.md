# Правила работы с проектом Dark Peony

Сайт публикуется на GitHub Pages из ветки `main`:
https://fuzzyfoxxyfoxx.github.io/Dark-Peony/
Архитектура и устройство морфинга описаны в `docs/ARCHITECTURE.md`.
Отложенные вопросы (решили вернуться позже) — `docs/DEFERRED.md`: заносить туда всё, что всплывает по ходу,
но сейчас не делается; перед переходом к новому этапу — просматривать.

## 1. Работаем прямо в main

После каждой правки делать коммит и пуш в `main` (`git push origin HEAD:main`),
чтобы изменения сразу появлялись на GitHub Pages. Перед пушем подтянуть `origin/main`.

## 2. Проверка визуала и FPS

- **В облаке (без GPU)** скриншоты рендера НЕ делать: это очень медленно. Проверять только,
  что в консоли нет ошибок и шейдеры компилируются (headless Chromium, `?q=low`, маленькое окно,
  `?manual` + `DP.debug.step(сек)`; проверять WebGL1 `?webgl1`, режим MESH и старт морфинга).
- **Локально (VS Code + Chrome с GPU)** можно и нужно смотреть самому: MCP-сервер `chrome-devtools`
  подключён в `.mcp.json`. Открывать сайт (локальный `index.html` или GitHub Pages) с `?debug`,
  делать скриншоты на разных стадиях морфинга, смотреть FPS и консоль. Итоговую оценку всё равно
  даёт автор на Mac и iPhone.
- Для кадров конкретного момента морфинга: `?manual`, затем в консоли
  `DP.morphTo('peony'); DP.debug.step(4)` — кадр на 4-й секунде (время стоит, пока не вызван step).
- FPS-счётчик на сайте: параметр `?debug` (FPS, WebGL1/2, качество, pixelRatio, число точек).

## 3. «Запомни» / «сохрани» / «сделай дубликат»

1. Скопировать текущую версию сайта в `versions/ГГГГ-ММ-ДД_ЧЧ-ММ_короткое-описание/`
   (всё, что нужно для работы: `index.html`, `css/`, `js/`, `vendor/`). Все пути внутри
   копии относительные, чтобы она открывалась по своей ссылке:
   `https://fuzzyfoxxyfoxx.github.io/Dark-Peony/versions/<папка>/`
2. Поставить git-тег с тем же именем (`git tag <папка> && git push origin <папка>`).
3. Добавить запись в `versions/README.md`.
4. Прислать автору ссылку на эту версию.

## 4. Список версий

`versions/README.md`: дата, ссылка и что в этой версии понравилось.

## 5. Время

Время в названиях версий и тегов — по Амстердаму (`TZ=Europe/Amsterdam date +%Y-%m-%d_%H-%M`), не UTC.

## 6. Возврат к сохранённой версии

Восстанавливать `main` из соответствующего тега: содержимое рабочих файлов берётся из тега
(`git checkout <тег> -- .` без удаления папки `versions/` и её README), затем обычный коммит
«Restore <тег>» и пуш в `main`. Историю `main` не переписывать (без force-push).

## Текущее состояние (для новой сессии)

- Пион перенесён из `archive/Gedal_web_preview_005_crossplatform.html` один в один — вид утверждён автором.
- Морфинг пион → пион (кнопка MORPH / пробел). Утверждено автором: распад и сборка идут
  от краёв к центру и низу (`assemble: 'outside-in'`), 360° (было 720°, автор попросил попробовать 360°), уход и посадка перекрываются.
- Отклонено автором: воронка/конус; уменьшение частиц в полёте (давало эффект «кометы наоборот»,
  сейчас `swirlSizeMin: 1`); равномерный «туман» в середине — частицы не видны, если не скучиваются.
- В работе: середина морфинга. Цель по референсам автора (Trapcode Particular в After Effects):
  плотные светящиеся жгуты, перепонки и ленты частиц, которые скручиваются в кольцо вокруг оси,
  плюс пыль вокруг. Сейчас: кольцо размером с цветок, сечение-«крыло» (`ring*`), частицы стянуты
  в тонкие жгуты (два шага Ньютона, поля в реальной позиции частицы), поля живут в потоке
  (`fieldSpin`, `fieldShear`, `clumpSpeed`). Волокна по уровням шума (`clumpFibers`) пробовали —
  давали муть, выключены. 360° вместо 720°; цветок непрерывно переходит в кольцо (не «наплывом»);
  лидеры с хвостами (`trail*`) дают штрихи вдоль потока. Цвет референса (золото) не интересует —
  только фактура.
  Автор: «частицы идут по ровным кругам» — добавлены перекрут ленты (`twist*`) и сильный двухволновой
  поток; «дальние лепестки вспыхивают при отрыве, как наплыв» — яркость вихря набирается поздно
  (`swirlBlend`), подсветка отрыва ослаблена (`leaveGlow`).
  Автор: «хаос, как УЗИ» → новый референс — распад человека в частицы: тонкие волнистые нити.
  Сделаны нити (`threadCell`): частицы ячейки сечения идут по одной линии. Стягивание, перекрут
  и хвосты выключены (давали хаос). Не решено: в момент срыва частицы идут дымкой, а не нитями,
  растущими из поверхности.
  Автор: «нити — треш, частицы щупалец не сводить в кольцо, пусть остаются на своём уровне и идут по
  синусоидам сильных полей»; референсы — видео распада черепа: поверхность срывается цельными вуалями.
  Сделано: `ringMode: false`, поле от исходной точки (слои цельные), нити выключены.
  Проверять плотным контактным листом: 16 кадров через 0.5 с на одном снимке (автор просит плотную
  раскадровку — по редким кадрам ошибки движения не видны). Кадр WebGL копируется в 2D-canvas сразу
  после `DP.debug.step()`.
  Найден и исправлен баг: оставшийся `twistSpeed` крутил сечение со временем → цветок схлопывался
  к центру (щупальца ныряли в центр), потом разлетался. Сейчас: частицы стекают, облетают Y на своих
  уровнях, поле умеренное (`flowAmp 0.3`) — вуали со складками, цветок не меняет размер.
  Щупальца — эталон автора: распадаются на частицы и собираются из потоков частиц. Этот распад даёт
  случайный разброс времени у каждой частицы (ORDER_JITTER, travelJitter) — его НЕ убирать
  (пробовали — распад пропал, автору не понравилось). «Дребезжание» при разборе давали только дрожь
  перед отрывом (`shiver`) и вспышка (`leaveGlow`) — они выключены (0).
  Поле нарастает вместе с поворотом, а не сразу (`fieldDelay`, `poseBlend`): сначала частица плавно
  уходит по кругу, потом среда её разбалтывает; при посадке поле гаснет раньше конца поворота.
  Поле течения переделано (вариант «A», без GPGPU): среда толкает, а не сдвигает — смещение = сумма
  толчков поля водоворотов (∇n1×∇n2) вдоль пройденного пути за `flowMemory` с (`flowSteps` отсчётов,
  по качеству 5/4/3); поле вращается с вихрем медленнее частиц (`fieldSpin`) и меняется во времени.
  Автор: флуктуации поля среды выглядят инородно → поле выключено (`flowAmp: 0`, код оставлен).
  Вместо него — «морская волна» (`wave*`): радиус не меняется, частица качается только по вертикали;
  фаза от места и времени (не случайная), поэтому соседи качаются вместе, без хаоса.
  Темп (по просьбе автора): уход ×1.4, вихрь ×5, посадка ×2 (`speedIn/speedMid/speedOut`),
  участки пути сшиты плавно; весь морфинг ≈ 8.3 с (было 12.7). Разбор и сборку по времени не трогать.
  Автор: волна и ускорение — «всё плохо», попросил попробовать реалистичный вариант B (GPGPU) и оценить
  нагрузку. Сделано: `js/core/flowsim.js` — симуляция среды на видеокарте (одна текстура отклонений,
  один проход в кадр). Замер на MacBook M3 Max: морфинг 67 FPS с симуляцией против 74 без неё
  (≈10% кадров); WebGL1 работает. iPhone не проверен — это решающая проверка.
  iPhone 15: симуляция не проседает (проверил автор). Внешний вид «не отличается от прежнего» →
  поле сделано попутным (`fieldSpin: 1` = средняя скорость вихря), сильнее (`flowAmp 1`, `flowFreq 0.5`,
  `simSpring 0.2`); введена «сплочённость» (`simCohesion`): у концов пути частица идёт по своему
  расписанию (распад/сборка по частицам, `orderJitter`, `travelJitter`), в вихре догоняет общий поток —
  полотна не размываются в туман, и водовороты рвут/скручивают их целиком.
  Автор против тяжёлых решений: сайт должен работать на разных устройствах. Если «A» не хватит —
  обсуждать с автором (возможно, другой принцип: волна, пробегающая по фигуре), GPGPU не начинать без согласия.
  Отложенный шаг: лепестки распадаются «нитями» как щупальца — полоски вдоль
  лепестка со своим сдвигом времени, внутри полоски частицы уходят строго по очереди (лидер и хвост).
  Если этого мало — следующий шаг: частицы, которые помнят траекторию (шлейфы/streaks, GPGPU).
- Медуза готова (`js/figures/jellyfish.js`, эскизы автора): купол «вдавленная сфера» с каналами и
  ресничками, 5 лент (прямой край + длинный волнистый в рюшах, сверху в прозрачность), 12 нитей-щупалец.
  Размер подобран по видимой массе пиона. Взгляд чуть снизу — наклон сцены `stageTilt`.
  Правки по эскизу автора (вид снизу): купол незамкнутый (без внутренней чаши) + внутренний купол
  поменьше (второй слой для плотности); 6 лент у края внутреннего купола, точки как у лепестков
  пиона (сетка × 3 со сдвигом, френель); между лентами короткие «тычинки»; 12 длинных щупалец
  кольцом ближе к центру, трубки из колец как у пиона; лёгкое затемнение дальней стороны (`uDepth`).
  Доработки: внутренний купол почти дублирует внешний (95%/96%) — голова яркая, как сфера на референсе;
  ленты строго радиальные и целиком под куполом, рюши спокойные (оборка из плоскости, мало зубцов),
  по волнистой кромке отдельно бежит волна сверху вниз; щупальца и тычинки — трубки из колец как у пиона
  (широкое основание, сужение к концу), кольца в шейдере слегка наклонены к зрителю (`aRingC`) —
  сбоку видны овалами, а не чёрточками; реснички по краю купола убраны (давали хаос точек).
  Юбка под куполом (эскиз автора): подол с крупными складками, у стыка с куполом прозрачна, к краю видима,
  френель; две синусоиды — радиальная и вертикальная (нижний край). Ленты: рюши считаются в шейдере
  (`aRuf`) и бегут по кромке от основания к кончику, амплитуда меняется бегущими «пакетами»; вся лента
  качается отдельной волной; у каждой ленты свои скорость/частота/сила (как у лепестков пиона).
  Края лент и нижний край юбки светятся сильнее, чем у пиона (edgeGlow ×3, точки у края крупнее).
  Точки всех поверхностей медузы — как у лепестков пиона: ровная сетка (шаг ≈0.019 на экране), 3 точки на узел
  со сдвигом ±0.4 ячейки, без объёмного разброса — видны рядки (раньше было «ворсисто»). Купол — сетка
  «параллели × меридианы», у вершины меридианов вдвое меньше.
  ПЛАН АВТОРА: доводить медузу по частям, отключив остальное: купол → ленты → длинные тентакли →
  короткие (тычинки) → юбка, потом всё вместе. Показ частей: `?figure=jellyfish&parts=bell`
  (bell, ribbons, tentacles, stamens, skirt — через запятую). По умолчанию сейчас показываются только
  части текущего этапа — константа `DEFAULT_PARTS` в `js/figures/jellyfish.js` (`''` — все части).
  Купол (этап 1): против «провала» в центре — базовая видимость лицевой стороны, «свет сверху» (сферичность),
  ярче каналы и крупнее точки на гребне, каналы внутреннего купола сдвинуты на полшага. Параметры —
  `DP.jellyfish.bellLook` (x база, y свет, z каналы, w размер); по умолчанию вариант «C».
  Загиб края внутрь: последние 5% профиля уходят в ноль по прозрачности (оба купола, точки и MESH).
  Градиент как у пиона: белое внизу, в широкой части купола; к вершине — лёгкая синева.
  Юбка (этап 2): два слоя (второй меньшего радиуса, свои складки и фаза, частично пересекаются); «гребок» в такт
  пульсации купола (подол сжимается внутрь и вверх) + волны по радиусу и вверх-вниз; верх юбки неподвижен;
  базовая видимость лицевой стороны поднята (против провала).
  Юбка, правка по разбору автора: острые пики давали волнистый срез подола + вторая гармоника складок +
  вертикальная волна 9/круг. Теперь: срез почти ровный, складки — одна плавная синусоида по радиусу (лишняя
  ширина подола собирается в складки), при «гребке» складки углубляются; волны пологие (2–5 на круг).
  Юбка, 3-я итерация: профиль — дуга ПОСТОЯННОЙ длины (не растягивается); анимируется только кривизна:
  гребок подворачивает подол под себя, поперечная волна по кругу то подворачивает, то выворачивает наружу;
  складки бегут по кругу. Обе юбки крепятся под куполом (r0 = 0.88 и 0.76 радиуса края).
  Юбка, 4-я итерация: рюшей много (22 и 17), подол шире; рюши заметно бегут по кругу; гребок — импульс от купола
  к краю хлыстом (запаздывание вдоль длины, профиль интегрируется в шейдере 12 шагами): медленно подбирается
  («фонарик», ease in-out), резко выпрямляется с выворотом («колокольчик»), успокаивается; ритм = пульс купола.
  Юбка, 5-я итерация (видео руки автора, IMG_9989): не «ставни», а волна изгиба бежит от купола к подолу
  (кривизна = sin(ωt − 3.4·u), к подолу сильнее, подворот сильнее выворота) — кончик описывает петлю.
  Рюши с природным рандомайзом: огибающая высоты, фазовые искажения и мелкая вторая гармоника, всё меняется во времени.
  Юбка, 6-я итерация (дыхание автор одобрил): форма рюшей нерегулярная — функция skFold (GLSL) / skirtFoldJS (CPU),
  одна формула: неровный шаг складок, высота ±50%, возвращена умеренная волнистость среза подола (hemAmp) +
  провисание краёв; всё медленно дрейфует.
  Юбка одобрена автором. Этап 3 — ленты (`DEFAULT_PARTS = 'ribbons'`). Вопрос о темпе юбки — в `docs/DEFERRED.md`.
  Ленты, 1-я правка этапа: у крепления рюши слабые и длиннее (ruffleWarp, амплитуда нарастает к середине);
  лента опущена и дольше выходит из «тени» под куполом; прямой край светлее; выравнивание видимости —
  лента, повёрнутая к камере ребром, гасится (плотность точек ∝ 1/|cos|), лицевые — ярче.
  Ленты были тяжелее лепестков пиона и «шумели»: точек на узел 2 вместо 3, сдвиг ±0.275 ячейки — рядки с живым разбросом, как у пиона,
  прозрачность ниже. Сейчас показываются купол + юбка + ленты (`DEFAULT_PARTS`).
  Ленты: вернули френель как у лепестков (лицевая сторона тусклее, изгибы/рюши светятся); уже на 15%, длиннее на 20%
  (уходят за край кадра), рюши сильнее (ruffleAmp 0.19, ruffleK 18–22).
  Ленты: сдвиг точек ±0.4 как у пиона; рюши — скруглённые лопасти с «шейками» и нависанием вдоль длины
  (эскиз автора), размер лопастей плавно гуляет; формула одна в ribbonPoint (CPU) и ribbonDisplacement (GLSL).
  Рюши лент — лопасти ribbonLobe / dpLobe: сначала Безье (автор: «пила»), теперь симметричная «луковица»: шире основания,
  скруглённая, чуть свисает к кончику. Откат к синусоиде — версия 2718381.
  По эскизу автора (зелёное против красного): лопастей мало (3–5 по длине, ruffleK 6.5–9), крупные и разные
  (высота 0.35–1.5), ≈45% лопастей заворачиваются — нависают и уходят из плоскости (складка), + мелкая рябь к кончику. Было: верх лопасти нависает над
  следующей «шейкой»; высота и нависание — свои у каждой лопасти (хэш номера), лопасть бежит вместе с формой.
  Контур ленты светится сильнее и меньше гасится компенсацией «ребром» — ленты ровнее по видимости.
  Итог по форме кромки: лопасти (Безье, «луковицы», крупные со складками) и 2D-эскизы «пазл/замочная скважина»
  (маска: тело ∪ круги − выемки) автор отклонил → возвращена синусоида (как в 2718381: ruffleK 18–22, ruffleAmp 0.19);
  сдвиг точек ±0.4 и усиленное свечение контура оставлены.
  Синусоида с природным разбросом (автор: «слишком математически»): длина волны гуляет (фаза искажена двумя
  медленными синусами, местная частота 0.27–1.73), размах ∝ k^−1.3 (короткие волны вдвое ниже, длинные выше),
  по длинным волнам — мелкая рябь (×2.3). Функция dpRibbonWave (GLSL) / ribbonWaveJS (CPU); ruffleK 15–19, ruffleAmp 0.17.
  Автор: вершины острые (размах ≈ длине волны), шаг регулярный → волна длиннее (ruffleK 10–12.5, ruffleAmp 0.2),
  размах ≈ ¼ длины волны — вершины скруглённые, как на эскизе автора (зелёная линия); искажение шага чаще
  (0.45/0.21 на радиан), рябь слабее (0.18).
  Рюши лент — кривая Безье по вершинам (эскиз автора из Illustrator): вершины чередуются влево-вправо, высота своя
  у каждой (0.45–1.2, хэш номера), расстояние между вершинами гуляет; между вершинами — дуга Безье с ручками вдоль
  ленты (1/3 участка = smoothstep), мелкой ряби нет. dpBezWave (GLSL) / bezWaveJS (CPU). Кривая без загибов назад —
  поэтому сетка точек не складывается (прошлые «Безье»/лопасти с нависанием давали «пилу» именно из-за загиба).
  Ленты одобрены автором («наконец-то сделали хорошо»). Этап 4 — длинные щупальца: `DEFAULT_PARTS = 'tentacles'`.
  Щупальца (автор): кольца не разворачивать к зрителю (aRingC убран) — овалами их делает наклон самой медузы
  (`stageTilt` −0.95 → −1.25, взгляд снизу ≈26°; было ≈8°); 9 щупалец внешним кольцом (r 0.42–0.56 внутр. купола),
  3 — ближе к центру (r 0.12–0.22) и на ~16% длиннее; в среднем щупальца на 20% длиннее лент, у каждого свой разброс
  длины (±12%), угла и радиуса. `TENTACLE_OUTER = 9`.
  Кольца щупалец медузы нанизаны на нить: плоскость кольца перпендикулярна нити (рамки параллельного переноса,
  aRingC + aTan), шейдер поворачивает каждое кольцо вслед за текущим изгибом качающейся нити (dpTentSway, кратчайший
  поворот касательной). У пиона кольца горизонтальные — не трогаем (автор).
  Щупальца одобрены автором. Этап 5 — короткие «тычинки» между лентами: `DEFAULT_PARTS = 'stamens'`.
  Тычинки: тоньше (TUBE_R×0.55), кольца чаще (шаг ×0.6), качание втрое слабее (атрибут aSwayK = 0.33), разброс шага
  по кругу, радиуса, высоты крепления (до −0.14) и длины (0.55–1.15).
  Тычинки одобрены. Этап 6 — медуза целиком (`DEFAULT_PARTS = ''`). Возможно, тычинки сделать менее заметными, если в сборке перетягивают внимание.
  МЕДУЗА ЦЕЛИКОМ ОДОБРЕНА АВТОРОМ (2026-09-25): «выглядит невероятно шикарно». Вид (включая stageTilt −1.25) не менять без просьбы автора.
- Морфинг, новый принцип (эскиз автора, 2026-09-25): «фонтан» — `DP.config.morph.mode = 'fountain'` (вихрь остался:
  `mode: 'vortex'`). Частица осыпается вниз по центральному столбу, у дна уходит наружу, поднимается по стенке сферы,
  через верх падает сверху на своё место (петли, как силовые линии магнита / вихрь Хилла), строго в своей вертикальной
  плоскости. Разрушение сверху вниз, сборка тоже сверху вниз, с перекрытием. Пары — по секторам по кругу (planFountain),
  внутри сектора сверху вниз. Струи: азимут сходится к одной из `jets` струй; 3 вложенные петли (`shells`).
  Петля — «суперэллипс» (`box`), размеры от габаритов фигур (`wallR`, `heightK`). Время ≈ 2.4 с (автор: 2 с — желательно,
  3–4 с допустимо, если красиво). Симуляция среды в этом режиме выключена.
  Автор: струи фонтана — «ровные шлейфы, ненатурально»; нужен живой дым (референс — дымное кольцо; The Spirit
  Эдана Квана — «ровно то, что нужно»). Сначала отдельно строим само кольцо, понимаем его физику, потом переносим
  принципы в морфинг. Лаборатория `lab/smoke.html` (ползунки, «ссылка с настройками», вращение мышью/пальцем,
  `?manual` + `SMOKE.step(сек)`): частицы на видеокарте (положение + возраст), течение = вихревое кольцо
  (Лэмб — Озеен вокруг сердцевины тора + бег вдоль кольца) + водовороты двух размеров (∇n1×∇n2), время жизни
  как в Particular (появление/угасание по кривым, рост к концу), умершая частица рождается в сердцевине, число
  частиц постоянно; часть частиц отрывается и уходит вверх. Сейчас дым мягкий, но «облаками», а не шёлковыми нитями.
  Добавлены ползунки «скорость частиц» (течение быстрее, жизнь та же) и «темп всего». По умолчанию 1 млн частиц
  (`?n=1024`; M3 Max — 120 FPS; для телефона проверить `?n=512`): мелкие тусклые частицы — дым глаже, без зерна.
- Панель настройки морфинга `?tune` (js/core/tune.js): цикл морфинга туда-обратно (пауза), замедление времени
  (`DP.timeScale`, `&slow=`), выбор режима (фонтан/вихрь), ползунки параметров из `DP.tuneSpec` (путь в DP.config.morph);
  «ссылка с настройками» — значения в адресе `m.<путь>=`, они применяются и без ?tune. Значения — со следующего морфинга.
  Следующий шаг (автор): морфинг через дымное кольцо — фигура рушится в кольцо, закручивается, из него собирается новая.
- Морфинг «дымное кольцо» — `mode: 'smoke'` (по умолчанию; фонтан и вихрь остались): js/core/smokesim.js — пиксель
  на пару, положение + возраст; до отрыва частица на фигуре A, отрыв сверху вниз (осыпание с ускорением), кольцо
  захватывает (притяжение к сердцевине), течение и жизнь — как в лаборатории (значения автора в `DP.config.morph.smoke`,
  единицы лаборатории, масштаб под фигуры), в срок частица садится на своё место в B (сверху вниз). Обе фигуры читают
  один пиксель — эстафета не рвётся. Пары — sectorPairs (общий с фонтаном). Нет float-текстур → фонтан.
  Время ≈ 3.3 с. Все параметры — на панели `?tune`.
- Морфинг «кольцо-кисть» — `mode: 'sweep'` (по умолчанию; «дымное кольцо» `smoke` оставлено для отката): дымное
  кольцо опускается сверху вниз (`sweep.descent`, ease) и «дышит» по силуэту обеих фигур на своей высоте (85-й
  процентиль радиуса по полосам); где проходит — старая фигура срывается (крайние раньше: `dome`, закрутка `twist`,
  срыв заранее `lead`), над кольцом из дыма проявляется новая (`dwell`). Пары — на одной высоте (сектор × полоса);
  лишние точки старой фигуры тают в кольце, недостающие новой — рождаются из кольца (идея автора). Наклон сцены
  меняется весь переход (`DP.morph.tiltWindow`). Время ≈ 2.4 с.
  Автор: кольцо тонкое, мало вихря, скачет по силуэту → кольцо «обволакивает сферу»: радиус по сфере вокруг фигур
  (`sphereR`; на полюсах 0, на экваторе максимум, без скачков), толщина сердцевины `core` растёт с кольцом, вращение
  кольца вокруг оси `rotate` (сильнее всего на экваторе), закрутка до захвата `twist` 3.5 — наверху из сорвавшихся
  точек закручивается спираль.
  Плотная раскадровка «кольца-кисти»: начало резкое (верх купола — плоский диск, потом голова разом в кольцо),
  конец — клубок щупалец внизу исчезает рывком. Автор: попробовать дымную СФЕРУ с «вчерашним» порядком.
- Морфинг «дымная сфера» — `mode: 'sphere'` (по умолчанию; sweep/smoke/fountain/vortex оставлены): распад по aOrder
  (от краёв элементов к середине и низу), закрутка и подхват в клубящийся дымный шар (вихрь Хилла `roll`, вращение
  `rotate`, водовороты ×`noiseK`, вылетевший дым тянет обратно `pull`), сборка новой фигуры из шара в том же порядке.
  Пары — по порядку распада (k-я уходящая ↔ k-я прилетающая). Параметры `DP.config.morph.sphere`, панель `?tune`.
  Автор настроил красивое начало («чай в стакане»: закрутка 7.25, подхват 2.5 с, клубление 0.25), но середина —
  «взболтанная масса», сборка — «из хаоса». Причины в коде: закрутка гасла после подхвата, частицы перерождались
  в случайных точках шара, посадка — по прямой. Сделано: закрутка держится весь полёт (`twistRamp` — разгон),
  перерождение в морфинге выключено (`respawn: 0`), сборка — зеркало распада: частица раскручивается из вихря
  по спирали в ту же сторону и садится (`spiral: 1`); значения автора — по умолчанию; полёт не короче посадки+0.4 с.
  Автор: при сборке вихрь не как у Квана — «однородная каша». Причина: пары по порядку распада = случайные в
  пространстве, пути соседей перекрещиваются. Сделано: пары «по месту» (`pairing: 'space'`, cellPairs — сектор ×
  полоса высоты): соседи летят вместе, лепестки собираются из прядей; лишние тают, недостающие рождаются в вихре;
  водовороты гаснут только к концу посадки (1 − land²). Середина пока зернистая — докручивать «хрусткость».
  «Хрусткость» (3 шага, автор: все три): (1) свой вид частиц в полёте у сферы — мельче и ярче (`flightSize/Alpha/Look`);
  (2) свои водовороты двух масштабов (`eddyBig*` гнут вихрь, `eddySmall*` рвут кромки на завитки) — раньше закрутка
  в ~8 раз сильнее водоворотов давала гладкие слои; теперь в середине тонкие нити; (3) самозатенение — следующим.
  (3) Самозатенение (как в The Spirit): точки пар рисуются камерой света (сверху-спереди-слева) в текстуру 256² —
  плотность дыма по 4 слоям глубины (каналы RGBA); частица в полёте темнеет по дыму между ней и светом
  (0.3 + 1.1·e^(−k·occ): кромки ярче, глубина до 30%). `sphere.shadow` (0.004; 0 — выкл.), stage.render → renderShadow.
  Работает и в WebGL1.
- Поэтапная доводка морфинга (автор, как медузу): этап 1 — чистый вихрь без водоворотов и полей Квана:
  `mode: 'disk'` (planDisk, diskFlow в smokesim). Распад по aOrder (от краёв к центру и низу) с закруткой, всё
  вращается «как чай в чашке» (скорость ~ r^spinPow, внутри быстрее) и стягивается в толстый диск с пустой серединой
  на экваторе; у каждой частицы своё место в толще диска (без скопления на границах), сечение — линза (бублик/галактика).
  Сборка — сверху вниз (по высоте точки B), посадка по спирали. Пары по месту (cellPairs). Параметры `DP.config.morph.disk`,
  раздел «Диск (чистый вихрь)» на панели. Следующие этапы: флуктуации, завихрения, поля Квана поверх.
  Автор: «из верхних собираются нижние и наоборот» → ползунок `disk.levels` (0 — диск на экваторе, 1 — «торнадо»: частица
  крутится на своей высоте, между местом в A и в B; верх → верх, низ → низ). Автор: «шикарно» → `levels: 1` по умолчанию.
  Этап 2 — завихрения Квана поверх «торнадо»: водовороты двух масштабов в diskFlow (`disk.eddy*`, мягкие по умолчанию:
  сильные разваливают торнадо в облако) + самозатенение (`disk.shadow` 0.002). Ползунки — «Диск: завихрения Квана».
  Обороты (замер `DP.smokeSim.probe`, 400 частиц): при прежней скорости медиана 2 оборота (720°), внутренние до 4.
  Ползунок `disk.turns` (среднее число оборотов; вращение и закрутка масштабируются). Итог у частицы ≈ целые обороты
  (возвращается в свою сторону — пары по месту), поэтому точность ±1.
  Автор: смена вида снизу/сверху во время морфинга (камера пересекает горизонталь около середины — вращение «разворачивается»)
  с завихрениями незаметна — оставить; «эхо» наклона после морфинга (фигура плавно доворачивается) — нравится, оставить.
  Вращение фигуры при морфинге (main.js, `DP.config.morph.figureSpin`): раскрутка в сторону вихря (≈30° к концу распада),
  докрутка по инерции и плавная остановка после морфинга (всего ≈210°). Панель — «Вращение фигуры».
  Автор: замирание на долю секунды при нажатии MORPH и «дёрганье перед остановкой» (в цикле это старт следующего
  морфинга). Причина — подготовка пар на главном потоке: 230–440 мс (сортировка 1.3 млн точек дважды + цикл заполнения
  с временными объектами + 60 МБ новых буферов). Сделано: ячейки кэшируются на раскладке, сортировку заранее считает
  фоновый поток (prewarm, Web Worker); цикл заполнения без аллокаций, буферы переиспользуются; весь план «диска»
  готовится заранее по частям ≤6 мс (planAhead: после show/morphend, для следующей фигуры по кругу), текстуры —
  заранее в видеокарту (smokeSim.stage). Если после подготовки двигали ползунки — план считается при нажатии (~70 мс).
  Итог: подготовка при нажатии 0–9 мс, самый длинный кадр старта 34–67 мс (было 233–443). Ошибка плана → показ
  новой фигуры без морфинга (не оставлять две фигуры). Фигуры отдают раскладку без экземпляра: `getLayout(ctx)`.
  Автор: «тычок» перед полной остановкой в конце. Найдено и сделано: (1) подготовка следующего перехода шла кусками
  по 6 мс через setTimeout и грузила 60 МБ текстур одним кадром — рвала ровность кадров (экран 120 Гц) как раз при
  докрутке → теперь только в простое (requestIdleCallback, 1–3 мс), текстуры данных — «мишени», загрузка полосами
  по 48 строк (smokeSim.stageJob); (2) в конце морфинга удалялись материалы/шейдеры старой фигуры (два кадра по 25 мс),
  а на старте следующего — создавались и собирались заново → запас экземпляров (orchestrator pool): фигуры
  переиспользуются; (3) нагрузка обрывалась разом (40→120 FPS в конце) → снижается постепенно: старая фигура
  перестаёт рисоваться после последней передачи эстафеты, симуляция и тени останавливаются после последней посадки
  (`DP.morph.phases`, режим точек).
  Автор: «проскок вперёд за долю секунды до остановки» (поворот и наклон чуть быстрее). Покадрово на ровном времени
  движение гладкое → причина в выводе на экран: при резком падении нагрузки кадры из очереди видеокарты выходят
  быстрее, чем посчитаны. Сделано: gl.finish() в моменты смены нагрузки (скрытие старой фигуры, остановка симуляции,
  конец морфинга — drainGPU в orchestrator), проход теней гаснет за 0.6 с (drawRange + сила, smokeSim.shadowFade).
  Оценка — за автором (из браузера вывод на экран не измерить).
- Фон (эскиз автора, 2026-09-25; окна «Я/СМЫСЛ/ФОРМА», тексты и цветовые квадраты эскиза — не делаем, окна — будущие
  пайплайны): HUD (stage.js drawHud) — две «весики»: малая (круг R + круги R сверху/снизу через центр, крестики на
  пересечениях) и большая (R2 = R·`hud.bigK` 1.57, выходит за экран; четырёхлучевые звёздочки на пересечениях); все
  линии одной толщины и цвета (`DP.config.hud`); крестики и звёздочки масштабируются с кругами (доли R).
  Галактика (`DP.config.background`): 360 редких крупных звёзд (было 900 мелких, почти невидимых), мерцают, диск
  медленно вращается; лёгкие облака дыма (26, мягкая неровная текстура, плывут и «дышат»). Панель `?tune` —
  раздел «Фон» (пути с «@» — от DP.config, применяются сразу через DP.background.sync).
  MORPH переключает пион ↔ медуза. Автор решил: доводку морфинга отложить до второй фигуры,
  пробовать новые принципы перехода (например, волна по фигуре) уже между пионом и медузой.
  Версия вихря перед медузой сохранена: `versions/2026-09-24_16-09_vortex-gpgpu`.
- Дальше по плану: ещё ~7 процедурных фигур (каждая в `js/figures/`), затем нодовая система
  поверх анимации и гамбургер-меню слева вверху.
